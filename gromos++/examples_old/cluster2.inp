# cluster2
	@rmsdmat    RMSDMAT.bin
	@cutoff     0.1
	@time       1 0.01
#	[@maxstruct <maximum number of structures to consider>]
#	[@human]    (use a human readable matrix)
#	[@ref]      (force clustering on the reference structure)

